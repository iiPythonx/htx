[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "htx"
dynamic = ["version"]
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
htx = "htx.__main__:main"

[tool.hatch.version]
path = "htx/__init__.py"
